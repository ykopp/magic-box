# VoxCPM2 is now MLX-native (mlx-community/VoxCPM2-*).
# No PyTorch / torchaudio / einops required.
#
# The MLX backend loads VoxCPM2 via mlx-audio just like Qwen.
# To download a VoxCPM2 MLX model:
#   huggingface-cli download mlx-community/VoxCPM2-8bit --local-dir models/VoxCPM2-8bit
#
# Or use the Model Manager in the Streamlit UI (magic-box-route tool).
