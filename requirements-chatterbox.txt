# Optional, isolated Chatterbox experiment dependencies.
#
# Recommended setup:
#   python3.11 -m venv .venv-chatterbox
#   source .venv-chatterbox/bin/activate
#   pip install -r requirements.txt
#   pip install -r requirements-chatterbox.txt
#
# Chatterbox depends on PyTorch/torchaudio. Keep it isolated from the main
# MLX/Qwen environment unless you explicitly want backend=chatterbox.
chatterbox-tts>=0.1.2
