# 播客音频生成工具 - 项目交接文档

## 项目概述

用户需求：创建一个单人播客，使用 AI 生成语音代替真人录音。

用户环境：
- MacBook Air M2
- 8GB 统一内存
- 512GB SSD

---

## 已完成的工作

### 1. 技术选型

经过多次测试，最终方案：

| 组件 | 选择 | 原因 |
|------|------|------|
| 框架 | MLX-Audio | Apple Silicon 优化 |
| 模型 | Qwen3-TTS 1.7B Base (8-bit) | 质量好，8GB 内存可运行 |
| 功能 | 语音克隆 | 支持克隆用户声音 |

### 2. 已安装的模型

| 模型 | 路径 | 大小 | 用途 |
|------|------|------|------|
| Qwen3-TTS 1.7B Base | `models/Qwen3-TTS-12Hz-1.7B-Base-8bit` | 3.1GB | 主要使用 |
| Qwen3-TTS 0.6B Base | `models/Qwen3-TTS-12Hz-0.6B-Base-8bit` | 1.3GB | 备用（更快但质量稍差） |
| Qwen3-TTS 0.6B CustomVoice | `models/Qwen3-TTS-12Hz-0.6B-CustomVoice-8bit` | 2GB | 不支持语音克隆，已弃用 |

### 3. 参考音频

| 文件 | 路径 | 时长 | 文本内容 |
|------|------|------|---------|
| 默认参考音频 | `audio_samples/龙湖安置小区 2.m4a` | 20秒 | "大家好，今天是个好日子。很高兴能和大家分享这些内容，希望对你们有所帮助。让我们开始吧。大家好，今天是个好日子。很高兴能和大家分享这些内容，希望对你们有所帮助。让我们开始吧。" |

### 4. 已创建的工具

| 文件 | 说明 |
|------|------|
| `podcast_generator.py` | 命令行播客生成工具 |
| `使用指南.md` | 详细使用文档 |

---

## 项目文件结构

```
/Users/liuchang/chenxi/qwen3-tts-apple-silicon/
├── .venv/                          # Python 虚拟环境
├── models/                         # 模型目录
│   ├── Qwen3-TTS-12Hz-1.7B-Base-8bit/    # 主模型 (推荐)
│   ├── Qwen3-TTS-12Hz-0.6B-Base-8bit/    # 备用模型
│   └── Qwen3-TTS-12Hz-0.6B-CustomVoice-8bit/
├── audio_samples/                  # 参考音频目录
│   ├── 龙湖安置小区 2.m4a          # 默认参考音频 (推荐)
│   ├── 龙湖安置小区.m4a            # 旧版本
│   └── Default_20260215-153307.mp3 # 最初版本
├── outputs/                        # 输出目录
│   └── *.wav                       # 生成的音频
├── podcast_generator.py            # 命令行工具
├── 使用指南.md                     # 使用文档
└── test_*.py                       # 各种测试脚本
```

---

## 关键技术参数

### 模型配置

```python
MODEL_PATH = "models/Qwen3-TTS-12Hz-1.7B-Base-8bit"
SAMPLE_RATE = 24000
```

### 生成参数（已优化）

```python
speed = 1.15        # 语速，用户满意
temperature = 1.0   # 随机性，控制自然度
max_chars = 80      # 分段长度，避免截断
```

### 参考音频文本

```
大家好，今天是个好日子。很高兴能和大家分享这些内容，希望对你们有所帮助。让我们开始吧。大家好，今天是个好日子。很高兴能和大家分享这些内容，希望对你们有所帮助。让我们开始吧。
```

---

## 性能数据

| 音频时长 | 生成耗时 | 速度比 |
|---------|---------|--------|
| 30 秒 | ~5 分钟 | 10x |
| 1 分钟 | ~5 分钟 | 5x |
| 1.5 分钟 | ~23 分钟 | 15x |

**注意**：生成速度波动较大，取决于文本分段和内存状态。

---

## 用户反馈总结

| 问题 | 解决方案 | 状态 |
|------|---------|------|
| 语速慢 | speed=1.15 | ✅ 已解决 |
| 声音低沉/压抑 | 更换参考音频 | ✅ 已解决 |
| 声音偏"娘" | 重新录制低沉有力的参考音频 | ✅ 已解决 |
| 机器感/不连贯 | 使用 1.7B 模型 + temperature=1.0 | ✅ 基本解决 |
| 长音频截断 | 分段生成后拼接 | ✅ 已解决 |

---

## 下一步任务

### 创建 Gradio Web 界面

用户希望创建一个交互式 Web 界面，功能需求：

1. **文本输入**
   - 文本框直接输入
   - 支持上传 .txt 文件

2. **参考音频**
   - 上传新的参考音频
   - 输入参考音频文本
   - 使用默认参考音频选项

3. **参数调整**
   - 语速滑块 (0.8 - 1.5)
   - 温度滑块 (0.8 - 1.3)

4. **生成功能**
   - 实时进度显示
   - 生成完成后预览音频
   - 下载按钮

5. **界面风格**
   - 中文界面
   - 简洁美观

### 推荐实现方案

使用 Gradio：

```bash
pip install gradio
```

预计代码量：~100 行

---

## 启动命令

```bash
cd /Users/liuchang/chenxi/qwen3-tts-apple-silicon
source .venv/bin/activate
```

---

## 注意事项

1. **内存限制**：8GB 内存运行 1.7B 模型接近极限，生成时不要开太多其他应用

2. **模型选择**：
   - 质量优先：使用 1.7B 模型
   - 速度优先：使用 0.6B 模型

3. **参考音频**：
   - 时长：10-20 秒最佳
   - 格式：MP3/M4A/WAV 都支持
   - 需要：准确对应的文本内容

4. **文本分段**：
   - 自动按句子分段
   - 每段不超过 80 字符
   - 分段后拼接成完整音频

---

## 联系上下文

- 项目目录：`/Users/liuchang/chenxi/qwen3-tts-apple-silicon`
- 虚拟环境：`.venv`
- 用户语言：中文
- 用户设备：MacBook Air M2, 8GB
