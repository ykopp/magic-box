# Magic Box - 项目交接文档

## 当前状态

Magic Box 已收敛为稳定的本地播客生成工具，当前版本 `V2.3.0`：

- 正式 Web 入口：`streamlit_app.py`
- CLI 入口：`podcast_generator.py`
- 主模型：`Qwen3-TTS-12Hz-1.7B-Base-bf16` 或本机已下载的 `Qwen3-TTS-12Hz-1.7B-Base-8bit`
- 备用模型：`models/Qwen3-TTS-12Hz-0.6B-Base-8bit`
- 生成结果目录：`outputs/`
- 本机参考音频目录：`audio_samples/`（公开仓库只保留 `.gitkeep`）
- 用户声音 profile 目录：`voices/profiles/`

旧 Gradio UI、旧交互菜单、PyInstaller 打包产物、个人声音素材、历史生成音频和临时日志已从公开版清理。

## 核心能力

- 长文播客生成，自动切分并拼接 WAV。
- checkpoint / resume，支持中断后继续。
- URL 文章正文抽取，并清理网页广告、订阅、分享、相关阅读等杂质，整理成播客稿。
- 多人源声音 profile 切换，用于不同人的 voice clone。
- 表达预设和高级微调，用于控制语速、随机性和段落长度。
- Qwen clone 的 `speed=1.00` 会按参考音频语速校准；真实模型 speed 写入 `quality_report.json`。
- 输出 WAV、MP3，或同时输出两种格式。

## 最近修复和维护重点

- Qwen voice clone 现在只走 Base 模型主线；`CustomVoice` / `VoiceDesign` 不再作为稳定克隆入口，避免选错模型类型。
- 模型完整性校验已补齐：加载和下载后都会检查 `config.json`、`model.safetensors`、`speech_tokenizer/config.json`、`speech_tokenizer/model.safetensors`。
- 缺少 `speech_tokenizer/model.safetensors` 会导致输出噪音；当前版本会提前失败并提示重新运行 `download_model.py`。
- 默认输出改为 MP3；Web 和 CLI 均支持 `mp3`、`wav`、`both`。MP3 依赖 `ffmpeg`。
- 响度标准化使用 -23 LUFS + peak limiter，post-loudness de-click 只处理 >0.5 的硬跳变，降低 clipping/爆音和过度修正风险。
- 增加音频健康检查：写入前后提示空音频、低 RMS、低 peak、NaN/Inf、接近 clipping。
- 保留后端真实 sample rate，并在拼接时校验各片段采样率一致。
- 生成链路保留每段 `_seg_*.wav` 证据和 `quality_report.json`；质量门禁失败时不会把坏音频当成成功结果。
- 依赖升级到稳定发布版本：`mlx==0.31.2`、`mlx-audio==0.4.3`、`mlx-lm==0.31.3`、`mlx-metal==0.31.2`、`transformers==5.8.1`、`huggingface_hub==1.15.0`。

## 声音素材规则

公开版不内置任何个人声音素材。用户需要临时上传自己的参考音频，或在“管理声音 Profile”里保存自己的样本。

`audio_samples/` 和 `voices/profiles/` 里的真实人声素材默认被 Git 忽略；只提交 `.gitkeep` 占位，避免把个人声音上传到 GitHub。

参考音频建议控制在 3-20 秒，使用清晰单人朗读，参考文本必须尽量逐字匹配音频内容。当前生成入口会在正式推理前拒绝太短、太长、静音、低电平或接近 clipping 的参考音频，避免继续生成明显异常的 voice clone 结果。

## 启动

```bash
cd "/Users/liuchang/Workspaces/Magic Box"
./.venv/bin/python -m streamlit run streamlit_app.py --server.address 127.0.0.1 --server.port 8507
```

不要使用系统 `python3 -m streamlit`；Qwen / VoxCPM 后端需要项目 `.venv` 中的 `mlx-audio==0.4.3`，旧环境会导致生成音频固定空洞和断续。

CLI：

```bash
.venv/bin/python podcast_generator.py --help
```

重新下载并校验主模型：

```bash
.venv/bin/python download_model.py 1
```

## 已知性能耗时

本地推理不是实时生成，耗时和内存状态高度相关。历史参考：

| 输出音频时长 | 生成耗时 | 建议 |
|---|---:|---|
| 30 秒 | 约 5 分钟 | 适合试听参数 |
| 1-1.5 分钟 | 约 5-23 分钟 | 生成前关闭重型后台任务 |
| 10 分钟 | 约 2.5 小时 | 固定输出文件名并使用 checkpoint/resume |

长文生成时建议接电运行；如果中断，保持同一输出文件名并启用 resume。

## 清理边界

可以清理：

- `outputs/` 下除 `.gitkeep` 外的生成文件
- `audio_samples/` 下除 `.gitkeep` 外的个人参考音频
- `voices/profiles/` 下不用的个人声音 profile；当前正在使用的 Profile 应保留在本机
- `runtime/`
- `build/`、`dist/`、`pyinstaller_cache/`
- `__pycache__/`、`.pytest_cache/`
- `*.log`、`.DS_Store`、临时 `qwen_tts_*.wav`

不要清理：

- `models/`
- `.venv/`
- `audio_samples/.gitkeep`
- `voices/profiles/.gitkeep`
- `outputs/.gitkeep`
- `voices/.gitkeep`

## 验证命令

```bash
.venv/bin/python -m py_compile streamlit_app.py voice_profiles.py voice_controls.py article_extractor.py podcast_generator.py
.venv/bin/python -m pytest -q
```
