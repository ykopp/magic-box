# Magic Box - 使用指南

## 启动 Web App

```bash
cd "/Users/liuchang/Apprun/chenxi/Magic Box"
source .venv/bin/activate
streamlit run streamlit_app.py
```

页面里可以粘贴稿件、导入 TXT、抽取 URL 正文、上传或选择自己的“克隆声音 Profile”，然后生成音频。
URL 抽取会清理广告、订阅、分享、相关阅读等网页杂质，并生成更适合朗读的播客稿。
输出格式可以选 `WAV`、`MP3` 或 `WAV + MP3`。
默认输出是 `MP3`；如果要进 Audition 或其它 DAW 做后期，建议选 `WAV + MP3`，同时保留 WAV 中间文件和可分享的 MP3。

## 切换不同人的声音

- 公开版不内置任何个人声音素材。
- 临时测试时，选择“临时上传源声音”，并填写源声音里实际说的文字。
- 长期复用时，在“管理声音 Profile”里上传源音频并填写对应文字。
- `audio_samples/` 和 `voices/profiles/` 里的个人音频默认被 Git 忽略，不要提交到公开仓库。

建议源音频 5-20 秒，清晰、无明显噪声，语气越接近目标播客状态，克隆结果越自然。

## CLI 生成

```bash
.venv/bin/python podcast_generator.py \
  --file podcast.txt \
  --ref-audio path/to/your_voice.wav \
  --ref-text "参考音频里说的完整文字" \
  --output outputs/episode_001.mp3 \
  --format both
```

常用参数：

| 参数 | 说明 |
|---|---|
| `--text` / `--file` | 直接输入文本或读取稿件文件 |
| `--output` | 输出路径；默认自动生成 `.mp3` 文件名 |
| `--ref-audio` | 克隆源声音 |
| `--ref-text` | 源声音对应文本 |
| `--speed` | 语速 |
| `--temperature` | 表达随机性 |
| `--format` | 输出格式：`wav`、`mp3`、`both`，默认 `mp3` |
| `--resume` | 从断点继续 |
| `--no-normalise` | 不做响度标准化 |

## 模型检查和噪音问题

如果生成结果接近噪音，优先检查 Qwen 模型是否完整。当前版本会在加载模型前校验这些文件：

```text
config.json
model.safetensors
speech_tokenizer/config.json
speech_tokenizer/model.safetensors
```

其中 `speech_tokenizer/model.safetensors` 缺失时最容易出现噪音音频。重新下载主模型：

```bash
.venv/bin/python download_model.py 1
```

如果要输出 MP3，本机还需要安装 `ffmpeg`：

```bash
brew install ffmpeg
```

## 音频质量和耗时

当前版本会在响度标准化后使用 limiter 降峰，减少 clipping；生成前后也会提示空音频、过低音量、接近 clipping 等音频健康警告。

本地生成耗时较长，历史参考：

| 音频时长 | 生成耗时 |
|---|---:|
| 30 秒 | 约 5 分钟 |
| 1-1.5 分钟 | 约 5-23 分钟 |
| 10 分钟 | 约 2.5 小时 |

长文建议固定输出文件名，并在中断后使用 `--resume` 继续。

## 清理说明

`outputs/` 是生成结果目录，可以随时清空，只保留 `.gitkeep`。
`audio_samples/` 和 `voices/profiles/` 是本机个人声音素材目录，公开仓库只保留 `.gitkeep`。
不要删除 `models/` 和 `.venv/`，除非准备重新下载模型、重装环境。
