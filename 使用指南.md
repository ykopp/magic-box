# 播客音频生成工具 - 使用指南

## 快速开始

### 1. 打开终端，进入项目目录

```bash
cd /Users/liuchang/Apprun/chenxi/qwen3-tts-apple-silicon
source .venv/bin/activate
```

### 2. 准备文本文件

创建一个文本文件（如 `podcast.txt`），写入你的播客内容：

```
大家好，欢迎收听今天的播客节目！
今天我们要聊的话题是...
...
感谢收听，我们下期再见！
```

### 3. 生成音频

```bash
python podcast_generator.py --file podcast.txt --output my_podcast.wav
```

---

## 命令参数说明

| 参数 | 简写 | 说明 | 默认值 |
|------|------|------|--------|
| `--text` | `-t` | 直接输入文本内容 | - |
| `--file` | `-f` | 文本文件路径 | - |
| `--output` | `-o` | 输出音频路径 | `outputs/podcast.wav` |
| `--ref-audio` | `-r` | 参考音频路径 | `audio_samples/龙湖安置小区 2.m4a` |
| `--ref-text` | - | 参考音频文本 | (已设置) |
| `--speed` | `-s` | 语速 | `1.15` |
| `--temperature` | - | 随机性（越高越自然） | `1.0` |

---

## 使用示例

### 示例 1：从文本文件生成

```bash
python podcast_generator.py --file my_content.txt --output outputs/episode_001.wav
```

### 示例 2：直接输入文本

```bash
python podcast_generator.py --text "大家好，欢迎收听今天的节目！" --output test.wav
```

### 示例 3：调整语速

```bash
python podcast_generator.py --file podcast.txt --speed 1.2 --output fast_podcast.wav
```

### 示例 4：使用不同的参考音频

```bash
python podcast_generator.py --file podcast.txt --ref-audio audio_samples/my_voice.m4a --ref-text "参考音频的文本内容"
```

---

## 文件结构

```
qwen3-tts-apple-silicon/
├── podcast_generator.py      # 主程序
├── audio_samples/            # 参考音频目录
│   └── 龙湖安置小区 2.m4a    # 默认参考音频
├── outputs/                  # 输出目录
│   └── *.wav                 # 生成的音频
├── models/                   # 模型目录
│   └── Qwen3-TTS-12Hz-1.7B-Base-8bit/
└── .venv/                    # Python 虚拟环境
```

---

## 性能参考

| 音频时长 | 生成耗时 | 说明 |
|---------|---------|------|
| 30 秒 | ~5 分钟 | 短片段 |
| 1.5 分钟 | ~23 分钟 | 中等长度 |
| 10 分钟 | ~2.5 小时 | 完整播客 |

**建议**：生成长音频时，可以在不需要电脑时运行（如晚上）。

---

## 常见问题

### Q: 如何更换我的声音？

1. 录制一段 10-20 秒的参考音频
2. 保存到 `audio_samples/` 目录
3. 使用 `--ref-audio` 和 `--ref-text` 参数指定

### Q: 声音太机器感怎么办？

尝试提高 `--temperature` 参数：
```bash
python podcast_generator.py --file podcast.txt --temperature 1.2
```

### Q: 语速太快/太慢？

调整 `--speed` 参数：
```bash
# 更快
python podcast_generator.py --file podcast.txt --speed 1.3

# 更慢
python podcast_generator.py --file podcast.txt --speed 1.0
```

### Q: 内存不足？

使用更小的模型（0.6B）：
```bash
# 编辑 podcast_generator.py，将 MODEL_PATH 改为：
MODEL_PATH = "models/Qwen3-TTS-12Hz-0.6B-Base-8bit"
```

---

## 更换参考音频

如果需要更换参考音频：

1. 录制新音频（10-20秒，清晰无噪音）
2. 保存到 `audio_samples/` 目录
3. 记住音频的文本内容
4. 运行时指定：

```bash
python podcast_generator.py \
  --file podcast.txt \
  --ref-audio audio_samples/新音频.m4a \
  --ref-text "新音频的文本内容"
```

---

## 技术规格

- **模型**: Qwen3-TTS 1.7B (8-bit 量化)
- **采样率**: 24000 Hz
- **支持格式**: 输入 MP3/M4A/WAV，输出 WAV
- **内存需求**: ~7 GB
- **平台**: Apple Silicon (M1/M2/M3)
